# API Adapters

`lib/adapters.ts` defines interfaces for:

- AI provider
- Product/PIM provider
- Database provider
- File storage provider
- Email provider
- Map provider
- Analytics provider

All default implementations are mocks so the app remains functional without paid API keys.
